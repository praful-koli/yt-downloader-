services:
  - type: web
    name: ytgrab
    runtime: node
    plan: free
    buildCommand: npm install && pip install -U yt-dlp && node build.js
    startCommand: node server.js
    envVars:
      - key: NODE_ENV
        value: production
